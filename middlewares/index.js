const authenticate = require('./authenticate');
// const validate = require('./validate');
// const { validateUser, validateUserByEmail } = require('./utils');

module.exports = {
  authenticate,
  // validate,
  // validateUser,
  // validateUserByEmail,
};
